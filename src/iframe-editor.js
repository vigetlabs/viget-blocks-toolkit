/**
 * Editor canvas (iframe) only — keep dependencies limited to handles WordPress
 * loads inside the iframed block editor; see block-styles.js / block-variations.js.
 */
import './features/block-patterns';
