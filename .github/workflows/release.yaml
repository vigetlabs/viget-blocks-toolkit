name: Create Release

on:
  push:
    tags:
      - 'v*' # Create release for any tag starting with 'v'

concurrency:
  group: release
  cancel-in-progress: true

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          # package-lock.json is an npm 11 lock: `npm ci` fails on Node 22
          # (npm 10.9) with 41 "Missing ... from lock file" errors. Same reason
          # ci.yaml reads .nvmrc - this file was missed when that landed, and
          # the v1.1.9 release build failed on it.
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build plugin assets
        run: npm run build

      - name: Build plugin ZIP
        run: npm run plugin-zip

      - name: Create GitHub Release
        run: |
          gh release create "$TAG" --verify-tag \
            --title "$TAG: $(date '+%Y-%m-%d')" \
            --generate-notes \
            --latest \
            "viget-blocks-toolkit.zip#Viget Blocks Toolkit"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
